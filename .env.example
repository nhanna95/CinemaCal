# Optional: Path to Google Calendar API credentials
# Download from Google Cloud Console after enabling Calendar API
# GOOGLE_CREDENTIALS_PATH=/path/to/credentials.json

# Optional: Path to store OAuth token (default: ~/.config/cinemacal/token.json)
# GOOGLE_TOKEN_PATH=/path/to/token.json

# Optional: Google Calendar ID for movie screenings (default: look up by name "Movie Screenings")
# This calendar is used when adding screenings. The Calendar view can show multiple calendars
# (meetings, classes, movie screenings, etc.); set this if the app can't find your
# "Movie Screenings" calendar (e.g. different name).
# Find the ID in Google Calendar: Settings → the calendar → Integrate calendar → Calendar ID
# GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com
